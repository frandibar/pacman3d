#ifndef __CLIENTUTILITIES_H__
#define __CLIENTUTILITIES_H__

#include <SDL/SDL.h>
#include <string>
#include <map>

///tipo de sonido a usar en el juego
typedef enum tSoundType {MODERN,CLASSIC,MUTE};

///mapa para corresponder teclas con mensajes de chat
typedef std::map<SDLKey, std::string> ShortcutMap;

///teclas con las que se movera el cliente
struct tMovementKeys {
    SDLKey forward;
    SDLKey turnBack;
    SDLKey left;
    SDLKey right;
};

#endif

